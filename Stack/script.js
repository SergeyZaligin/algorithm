/**
 * Вспомогательный класс содержит:
 * value -значение элемента списка и 
 * next - ссылку на следующий элемент
 */
class StackNode {
	constructoor (value) {
		this.value = value;
		this.next = null;
	}
}

/**
 * Класс Stack
 * root - корень 
 * n - кол-во элементов
 */
class Stack {
	constructoor () {
		this.root = null;
		this.n = null;
	}
}